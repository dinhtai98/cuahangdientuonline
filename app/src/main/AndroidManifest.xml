<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:dist="http://schemas.android.com/apk/distribution"
    package="com.example.cuahangdientuonline">
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />

    <dist:module dist:instant="true" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_icon"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activy.QuanLyAdmin"></activity>
        <activity android:name=".activy.QuanLySanPham" />
        <activity android:name=".activy.SuaSanPham" />
        <activity android:name=".activy.SanPhamActivity" />
        <activity android:name=".activy.Admin" />
        <activity android:name=".activy.ThemSanPham" />
        <activity android:name=".activy.CacDonHang" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".activy.ThongTinActivity"
            android:label="@string/title_activity_thong_tin" />
        <activity android:name=".activy.ThongTinKhachHang" />
        <activity android:name=".activy.GioHang" />
        <activity android:name=".activy.ChiTietSanPham" />
        <activity android:name=".activy.LienHeActivity" />
        <activity android:name=".activy.LaptopActivity" />
        <activity android:name=".activy.DienThoaiActivity" />
        <activity android:name=".activy.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>